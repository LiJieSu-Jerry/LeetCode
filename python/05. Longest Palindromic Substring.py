class Solution(object):
    def longestPalindrome(self, s):
        """
        :type s: str
        :rtype: str
        """
        
        left,right=0,0
        char=""
        longestChar=""
        for i in range(len(s)):
        	char+=s[i]
        	if char
        		
        return longestChar

a=""
print (len(a))